## Login Instructions (HTTP Basic Auth)
The target uses HTTP Basic Authentication.
- Username: {{username}}
- Password: {{password}}
Include the Authorization header with Basic base64(username:password) in all requests.
