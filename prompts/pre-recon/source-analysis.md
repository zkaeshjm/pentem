# Source Code Analysis
Target: {{target_url}}

You have access to the application's source code at the configured repository path.
Analyze the source code to identify:
1. Configuration files revealing endpoints, API keys, or internal services
2. Route definitions (Express, Flask, Django, Rails, etc.)
3. Database queries and ORM usage patterns
4. Authentication/authorization middleware
5. Known vulnerable libraries or dependencies
6. Comments, TODOs, or debug endpoints that should not be in production

Focus on finding entry points and potential vulnerabilities that can be confirmed through dynamic testing.
